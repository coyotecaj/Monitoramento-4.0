const fs = require('fs');

// We are going to just replace the whole Trips component with a basic placeholder that builds so I can start fresh, since it's hopelessly corrupted
// Or I can copy another file that is clean and patch it.

